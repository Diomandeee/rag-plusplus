# RAG++ Context Gateway — Lean Production Dependencies
# D4 fix: Gateway-only deps (no torch/transformers/faiss).
# D7 fix: Pinned versions from production Docker image.
# D3 fix: Only google-genai (not google-generativeai which is deprecated).
#
# Usage: pip install -r requirements-gateway.txt
# Image size reduction: ~7.5GB → ~500MB

# Core
numpy==2.4.2
pydantic==2.12.5
pydantic-settings==2.13.1
msgpack==1.1.2
xxhash==3.6.0
scipy==1.17.1
structlog==25.5.0

# FastAPI Service
fastapi==0.135.1
uvicorn[standard]==0.41.0
websockets==15.0.1
python-multipart==0.0.20

# HTTP Client (replaces aiohttp)
httpx==0.28.1

# Supabase
supabase==2.28.0

# Google Gemini (single SDK — D3 fix)
google-genai==1.66.0

# Observability
prometheus_client==0.24.1
